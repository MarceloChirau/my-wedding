import { dancingScript } from "../../components/ui/fonts";


export default function  Venue(){
    return (
        <p className={`${dancingScript.className} font-normal text-base p-1 text-olive-500 `} >Venue</p>
    )
}