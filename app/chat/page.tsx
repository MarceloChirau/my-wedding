import { dancingScript } from "../../components/ui/fonts";


export default function  Chat(){
    return (
        <p className={`${dancingScript.className} font-normal text-base p-1 text-olive-500 `} >Chat</p>
    )
}